Julia uses `SHA.sha1` from the SHA stdlib. `bytes2hex` converts the byte array to a hex string.

___

##### Run Command:
`julia sha1-hashes.jl`

##### Results:
`sha1 this string`
`cf23df2207d99a74fbe169e3eba035e633b65d94`
