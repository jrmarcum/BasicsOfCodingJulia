Julia uses `@enum` macro for enumerations. State names are printed via a lookup dict since Julia's `@enum` doesn't support custom string representations directly.

___

##### Run Command:
`julia enums.jl`

##### Results:
`connected`
`idle`
