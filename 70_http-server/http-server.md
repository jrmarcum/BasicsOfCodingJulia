Julia uses HTTP.jl with a `Router` for HTTP server routing. Requires `HTTP` package. Run then test with `curl localhost:8090/hello`.

___

##### Run Command:
`julia http-server.jl`

##### Results:
`[server runs on port 8090 until interrupted]`
