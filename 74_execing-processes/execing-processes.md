Julia uses `run()` to execute external processes. On Windows, `cmd /c dir` is used instead of `ls -a -l -h`. Output varies by system.

___

##### Run Command:
`julia execing-processes.jl`

##### Results:
`[directory listing output]`
