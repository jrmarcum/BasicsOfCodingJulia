Julia uses `sort` with a `by` keyword argument instead of Go's `sort.Slice` with a less function.

___

##### Run Command:
`julia sorting-by-functions.jl`

##### Results:
`["kiwi", "peach", "banana"]`
