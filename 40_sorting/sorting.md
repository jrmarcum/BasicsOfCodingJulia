Julia uses `sort!` for in-place sorting and `issorted` to check if a collection is already sorted. Arrays print with brackets and commas rather than Go's bracket-only format.

___

##### Run Command:
`julia sorting.jl`

##### Results:
`Strings: ["a", "b", "c"]`
`Ints:    [2, 4, 7]`
`Sorted:  true`
