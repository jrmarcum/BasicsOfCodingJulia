Julia uses abstract types and multiple dispatch instead of interfaces. Structs print as `TypeName(fields)` rather than Go's `{fields}`.

___

##### Run Command:
`julia interfaces.jl`

##### Results:
`Rect(3.0, 4.0)`
`12.0`
`14.0`
`Circle(5.0)`
`78.53981633974483`
`31.41592653589793`
