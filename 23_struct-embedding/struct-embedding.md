Julia uses composition with explicit field delegation rather than Go's implicit struct embedding.

___

##### Run Command:
`julia struct-embedding.jl`

##### Results:
`co={num: 1, str: some name}`
`also num: 1`
`describe: base with num=1`
`describer: base with num=1`
